# ns-site
Nick's Site

Personal resume site — a single static `index.html` (Tailwind via CDN). Deployed with GitHub Pages from the `main` branch root.

To enable: repo Settings → Pages → Source: "Deploy from a branch" → Branch: `main` / `/(root)`.

Drop `Nick-Severson-Resume.pdf` in the repo root for the download links to work.
